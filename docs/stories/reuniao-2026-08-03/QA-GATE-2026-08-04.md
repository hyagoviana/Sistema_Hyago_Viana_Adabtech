# QA Gate — Reunião 2026-08-03 (8 ajustes A1–A8)

- **Quality gate:** @qa (Quinn)
- **Data:** 2026-08-04
- **Escopo:** revisão de código contra os AC + gates automatizados + validação no banco (dev=prod).
- **Base:** working tree (não commitado) sobre `main@cfcbfbc`. Migrations 20260804000001–4 já APLICADAS; A8 já IMPORTADA em produção.

---

## Veredito por story

| Story | Veredito | Resumo |
|-------|----------|--------|
| **A1** — campo só na pipeline | **PASS** | Fluxo "adicionar campo" removido da ficha; preenchimento/`freeEntries`/`scope=cliente` intactos; def gateada por admin. |
| **A2** — ocultar do filtro | **PASS** | Coluna `hidden_in_filters` no banco + view `_active` + toggle/badge no editor + `filterableDefs` filtra. Ortogonal a `hidden_in_list`. |
| **A3** — múltiplos Kanbans | **PASS** | Migration aplicada (13 boards principais 1:1); RBAC RPC correto; `moveCaseInBoard` NÃO toca `macrostatus_op` (isolamento op/fin provado). Follow-ups declarados. |
| **A4** — duplicar/mover tema | **PASS** | Popup mover×duplicar com confirmação nominal; `duplicateCaseToTema` via `createCase`; gate `casos.manage`; eventos cruzados. |
| **A5** — checklist op + checkbox auto-avanço | **PASS (c/ observação)** | Causa-raiz 5a correta (painel nunca montado); 5b relaxa UI+trava ad-hoc; 5c auto-avanço com anti-loop. Migrations aplicadas. |
| **A6** — timeline no topo + eventos | **CONCERNS** | Topo + remoção do manual + `note_added` OK. **AC-6 (`signature_completed`) e AC-8 (`checklist_done`) NÃO implementados**; AC-9 (paginação) não implementado; `andamento_importado` sem rótulo. |
| **A7** — campos estruturados (fatia) | **PASS (fatia)** | AC4 (formatadores determinísticos) + AC5a (aviso de órfão) entregues e testados. AC1/AC2/AC3/AC5b deferidos (aguardam modelos do dono) — declarado. |
| **A8** — importação Mais Médicos | **PASS** | Contagens no banco batem 100% com a story; integridade referencial limpa; idempotência provada. |

**Bloqueante:** nenhum. Nada quebra em runtime. A6 tem AC não atendidos (não-bloqueantes).

---

## Gates automatizados

Rodados de dentro de `sistema-hv/`:

| Comando | Resultado |
|---------|-----------|
| `npx tsc --noEmit` | **PASS** — só 1 erro PRÉ-EXISTENTE (`src/lib/contaazul/service.ts:173`), exatamente o baseline esperado. |
| `npm run build` (vite) | **PASS** — `✓ built in 58.71s`, exit 0. |
| `npx tsx scripts/smoke-tema-fields.ts` | **PASS** — 13 passou / 0 falhou. Self-cleaning (soft-delete no finally). |
| `npm run test:validators` | **PASS** — todos verdes. |
| `npm run test:rbac` | **PASS** — todos verdes. |
| `npx tsx src/lib/cases/document-autofill.test.ts` | **PASS** — inclui os novos formatadores A7 (`formatPercentBR`/`formatMoneyBR`/`formatIntBR`) + aliases de % êxito. |
| `npx eslint` (23 arquivos das 8 stories) | **PASS** — 0 erros; 1 warning pré-existente `react-refresh` em `CaseFiltersPanel.tsx:474` (não introduzido por estas mudanças). |

Nenhum lixo deixado no banco pelos smokes.

---

## Validação no banco (dev=prod) — A8 e schema novo

`npx tsx scripts/db-query.ts`. Tema **Mais Médicos** `id=044c960d-47e5-43df-9dff-43ba825cc73e`.

| Métrica | Esperado (story) | Medido | OK |
|---------|------------------|--------|----|
| Clientes do batch `MM_2026_08_03` | 381 | **381** | ✅ |
| Casos (tema) `lifecycle=CLIENTE` | 381 | **381** | ✅ |
| Casos com `stage_op_id` nulo | 0 | **0** | ✅ |
| Campos do tema (`_field_defs`) | 21 | **21** | ✅ |
| Checklist-defs (tema) | 7 | 10 total (7 do tema + 3 fin globais) | ✅ |
| Notas | 476 | **476** | ✅ |
| Andamentos (`action='andamento_importado'`) | 180 | **180** | ✅ |
| Checklist-items | 2368 | **2368** | ✅ |
| `system_parcelas` do tema | 0 | **0** | ✅ |
| Clientes sem `drive_folder_id` | 0 | **0** | ✅ |
| Clientes com CPF-marcador `CL-%` | 381 | **381** | ✅ |
| Itens de checklist órfãos (def inexistente) | 0 | **0** | ✅ |

**Distribuição op** (bate com STATUS_CASO): Judicial 244 / Administrativo feito 122 / Documentos iniciais 8 / Inicial 2 / Encerrado 2 / Rescisão 2 / Stand by 1. ✅

**Schema novo confirmado no banco:**
- `system_tema_field_defs.hidden_in_filters` ✅ + `move_to_stage_slug` ✅ (ambos também na view `system_tema_field_defs_active` ✅ — crítico p/ A2/A5c que leem a view).
- `system_pipeline_boards` ✅ (13 boards, 100% `is_principal`, 1 por service_type distinto).
- `system_case_board_positions` ✅.
- `system_pipeline_stages.board_id` ✅.
- 4 migrations + 4 rollbacks presentes em `supabase/migrations|rollbacks/20260804000001..4`.

---

## Revisão de código — achados por story

### A1 — PASS
- `CaseCanonicalFields.tsx`: removidos `newKey`/`newValue`, `addField`, bloco JSX "Adicionar", imports `Plus`/`Button`. `saveDef`/`saveKey`/`freeEntries`/`scope=cliente`/multi-ocorrência intactos. Nenhum caminho de UI cria def a partir do caso.
- AC-4 (auditoria): a criação de def continua exclusiva do `TemaFieldDefsEditor` (gate admin). Do caso só `updateCaseCanonicalFields` (valor). Confirmado.

### A2 — PASS
- `CaseFiltersPanel.tsx:207` `filterableDefs` agora `.filter((d) => !d.hidden_in_filters)` — cobre Lista e Kanban (mesmo componente).
- Editor/serviço/RPC/hook/types fiados corretamente; badge "(fora do filtro)" e checkbox presentes.
- **Divergência de nomenclatura (não-bug):** a migration foi `20260804000001` (o AC-1 citava `20260803000001`). Reconhecido no Change Log; conteúdo idempotente correto (`ADD COLUMN IF NOT EXISTS` + recria view + grants).

### A3 — PASS
- `board-service.ts`: guardas 409 (delete board com casos / delete principal / delete etapa com casos), 422 (etapa inválida), tombstone de slug, `getVisibleCaseIds` na leitura.
- **Isolamento op/fin (regra dura):** `moveCaseInBoard` (linhas 392–402) escreve SÓ em `system_case_board_positions` (`stage_id`/`stage_slug`/`entered_at`) — nunca em `macrostatus_op`/`stage_op_id`. ✅
- `rpc/boards.ts`: reads=`requireAuth`; CRUD board/etapa=`requireModule("sistema","edit")`; mover/adicionar caso=`requireModule("operacional","edit")`; todos com Zod.
- Financeiro no `BoardSelector` só quando `can(role,"financeiro.manage")` (prop `onFinanceiro` condicional). ✅
- Follow-ups (reorder na UI, smoke UI Playwright, checklist em board custom) declarados — não bloqueiam.

### A4 — PASS
- `LinkCaseToTemaDialog.tsx`: modo mover×duplicar, destino exclui o tema atual, **confirmação nominal** (Alert com nomes origem→destino), toasts distintos, `opResetado` no toast do mover, estados de `isPending`.
- `duplicateCaseToTema`: via `createCase` (herda case_code/pasta Drive/lifecycle) + copia `canonical_fields` + eventos cruzados `duplicado_em_tema`/`duplicado_de_caso`. `moverCasoParaTema` ganhou `modo:"mover"`/`from_tema_id` no evento.
- RPC `duplicarCasoParaTemaFn` reusa `handleManage`/`moverParaTemaSchema`. ✅

### A5 — PASS (com observação)
- **5a:** causa-raiz correta e cirúrgica — `CaseChecklistPanel` nunca era montado na ficha; agora montado em `casos.$id.tsx` com `[macrostatus_op, macrostatus_fin]` (sem `NAO_APLICAVEL`).
- **5b:** `StageEditor.tsx:85` → `showChecklist = kind === "fin" || kind === "op"`; trava ad-hoc de op relaxada (`checklist-service.ts` — agora só rejeita slug inexistente `kinds.size===0`).
- **5c:** `maybeAutoAdvanceByCheckbox` em `updateCaseCanonicalFields` com todas as guardas (só patch, só transição não-sim→sim, só boolean com `move_to_stage_slug`, valida slug op real, anti-loop `!== macrostatus_op`, best-effort try/catch). Evento `stage_moved_by_checkbox`.
- **Observação (não-bug):** o auto-avanço grava DOIS eventos por movimento (`status_changed` via `moveCaseStatus` + `stage_moved_by_checkbox`). É intencional (carimba a origem) e documentado na story — mas o dono verá as duas linhas na timeline.

### A6 — CONCERNS
Entregue: timeline no topo (antes de `CaseDossie`), remoção do input manual, `note_added` (best-effort, com preview 140 chars) + rótulo. Gates verdes.

**AC NÃO atendidos (documentados como gaps, mas ainda são AC da story):**
1. **AC-6 `signature_completed`** — `zapsign/webhook.ts` NÃO insere evento de assinatura concluída (grep: 0 ocorrências). A conclusão da assinatura não aparece na timeline (só o envio `doc_sent_zapsign`, que já existia).
2. **AC-8 `checklist_done`** — `checklist-service.ts` só emite `checklist_inconsistente`; marcar um item concluído NÃO gera evento (grep: 0 ocorrências de `checklist_done`).
3. **AC-9 paginação/scroll p/ 100+** — `useCaseEvents` chama `listCaseEventsFn` com o default `limit=50`, sem "carregar mais". Baixo risco AGORA (máx. de eventos por caso Mais Médicos = 7), mas o mecanismo pedido não existe; casos antigos/ativos com >50 eventos truncariam silenciosamente.
4. **`andamento_importado` sem rótulo** — os 180 andamentos importados (A8) caem no `default: return e.action` e renderizam a string crua `"andamento_importado"`; a descrição real (em `diff`) não é exibida. Não quebra (sem crash), mas é feio e visível em toda ficha Mais Médicos.

### A7 — PASS (fatia AC4 + AC5a)
- `document-autofill.ts`: `formatPercentBR`/`formatMoneyBR`/`formatIntBR` determinísticos (testados); `augmentWithHonorarios` formata pelo TIPO, não pela string; novos aliases de "% êxito".
- `GenerateCaseDocumentFlow.tsx`: `orphanKeys` + aviso âmbar não-bloqueante por placeholder órfão. Fluxo de geração inalterado.
- AC1/AC2/AC3/AC5b explicitamente deferidos (aguardam revisão dos modelos Word do dono). Fatia honesta.

### A8 — PASS
- Todas as contagens batem (tabela acima). Integridade: 0 stage nulo, 0 checklist órfão, 0 cliente sem Drive, `system_parcelas`=0 (decisão 3 respeitada).
- CPF-marcador `CL-XXXX` em 100% (coluna não virou nullable). Idempotência provada pela story (re-exec = 0 inserts).
- Rollback + report do dry-run presentes (`A8-dry-run-report.md`, `import-mais-medicos-rollback.sql`).

---

## Bugs / concerns priorizados

| # | Sev | Story | Item | Arquivo |
|---|-----|-------|------|---------|
| 1 | Médio | A6 | AC-6: evento `signature_completed` não emitido na conclusão da assinatura ZapSign | `src/lib/zapsign/webhook.ts` |
| 2 | Médio | A6 | AC-8: evento `checklist_done` não emitido ao marcar item concluído | `src/lib/checklist-service.ts` (`marcarItemChecklist`, ~L661) |
| 3 | Baixo | A6/A8 | `andamento_importado` sem rótulo em `renderEventLabel` → mostra string crua na timeline dos 381 casos | `src/components/cases/CaseTimeline.tsx:131` (`default`) |
| 4 | Baixo | A6 | AC-9: sem paginação/"carregar mais"; `useCaseEvents` fixo em `limit=50` | `src/hooks/useCases.ts:70` + `cases-service.ts:1806` |
| 5 | Info | A2 | Migration nomeada `...000001` vs `...000803...` do AC (só nome; conteúdo correto/aplicado) | `supabase/migrations/20260804000001_...` |
| 6 | Info | A5 | Auto-avanço gera 2 eventos (`status_changed` + `stage_moved_by_checkbox`) — intencional, mas duplica linha visual | `src/lib/cases-service.ts` (`maybeAutoAdvanceByCheckbox`) |

**Nenhum item bloqueante.** #1–#4 são AC não atendidos de A6 (a story se declara "núcleo"); recomenda-se fechar #3 (1 linha) antes do teste manual do dono, já que os 381 casos importados exibem andamentos crus.

---

## Riscos residuais para o teste manual do dono
- **A6:** ao clicar num caso Mais Médicos, os andamentos aparecem como `andamento_importado` (texto cru) — esperado dado o gap #3. Assinaturas concluídas e itens de checklist marcados NÃO gerarão linha na timeline (gaps #1/#2).
- **A5c:** validar no localhost que marcar um checkbox boolean com destino configurado move o card (e que re-salvar "Sim" no destino é no-op). Smoke DB do dev cobriu; falta o clique real na UI.
- **A3:** smoke UI Playwright do DnD em board custom é follow-up declarado — o move foi validado só por código/smoke DB. Testar arrastar num board custom e confirmar que o card do board principal (op) não se mexe.
- **A7:** geração de documento com % / valor depende dos modelos Word do dono terem placeholders `<...>` (AC1/AC2 deferidos) — o motor está pronto, os modelos não foram revisados nesta fatia.
