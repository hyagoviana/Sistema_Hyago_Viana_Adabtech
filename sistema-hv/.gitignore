# Environment variables
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
.vercel
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Backups de cadastros (contêm CPF/dados pessoais — NUNCA versionar)
backup-cadastros-*.json

# De-para PES↔nome↔código do ProJuris (dados pessoais — NUNCA versionar)
scripts/depara-pes-codigo.json
scripts/colaboradores-normalizado.json

# smoke de UI (screenshots gerados)
.smoke-ui/
.smoke-ui-session/
# Segredos: nenhum .env entra no repositório (o arquivo `env` já vazou uma vez).
.env*
# ...menos o modelo sem valores, que serve de referência para quem monta o ambiente.
!.env.example
